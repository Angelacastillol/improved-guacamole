// Obtenemos el contenedor
const container = document.getElementById('name-container');

// Creamos un h1 con el texto "Angela"
//<h1> "Angela"</h1>;

// Agregamos el h1 al contenedor
container.appendChild(heading);
